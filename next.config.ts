import type { NextConfig } from "next";

const nextConfig: NextConfig = {
codex/create-new-website-for-sleeper-j8e4ya
  output: "export",
  images: {
    unoptimized: true,

  images: {
 main
    remotePatterns: [
      {
        protocol: "https",
        hostname: "images.unsplash.com"
      }
    ]
  }
};

export default nextConfig;
